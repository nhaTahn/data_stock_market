# Notebooks

These notebooks are exploratory entrypoints for the analysis utilities that now live under `experiments/`.

- `compare_target_modes.ipynb`: inspect target-mode summaries and candidate runs.
- `run_trial_comparison.ipynb`: compare metrics and backtest artifacts across multiple runs.
- `sector_feature_summary.ipynb`: review feature-search results by stock and sector.
- `vn_model_optimization_and_ood_readiness.ipynb`: one-page summary of current VN anchor choice, rank-sidecar probes, and JP/KR/US readiness.

Keep executable automation and repeatable packaging flows in `experiments/`. Use notebooks for inspection, hypothesis testing, and interactive reporting.
